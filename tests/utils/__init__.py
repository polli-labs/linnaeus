# Intentionally empty.
#
# Marking this directory as a package keeps pytest imports consistent with the
# rest of the test suite (e.g. tests/profiling), and avoids `ModuleNotFoundError`
# for local (non-installed) imports when running individual tests.

